---
phase: 09.1-design-change
plan: 03
type: execute
wave: 2
depends_on: ["09.1-01", "09.1-02"]
files_modified:
  - app/page.tsx
  - app/contact/page.tsx
  - app/koren/page.tsx
  - app/koren/[slug]/page.tsx
  - app/wie-zijn-wij/page.tsx
  - app/evenementen/page.tsx
  - app/galerij/page.tsx
  - app/galerij/[slug]/page.tsx
  - app/privacy/page.tsx
  - app/not-found.tsx
autonomous: false

must_haves:
  truths:
    - "Homepage hero uses purple accent color"
    - "All page headers use purple for emphasis"
    - "CTA sections use purple backgrounds"
    - "Event date badges use purple"
    - "All icon accent colors are purple"
    - "No coral color appears anywhere on the site"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with purple hero, CTAs, and accents"
      contains: "bg-purple"
    - path: "app/contact/page.tsx"
      provides: "Contact page with purple accents"
      contains: "text-purple"
    - path: "app/koren/page.tsx"
      provides: "Choir listing with purple accents"
      contains: "text-purple"
    - path: "app/evenementen/page.tsx"
      provides: "Events with purple date badges"
      contains: "bg-purple"
  key_links:
    - from: "app/page.tsx"
      to: "app/globals.css"
      via: "purple color classes"
      pattern: "bg-purple|text-purple"
---

<objective>
Update all page files to use purple color palette, then visually verify the complete design change.

Purpose: Complete the coral-to-purple migration across all pages, ensuring consistent branding throughout the entire site.

Output: All pages using purple, zero coral references site-wide, visual verification of the new design.
</objective>

<execution_context>
@C:\Users\lieve\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\lieve\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Projects\KoorFameus\.planning\phases\09.1-design-change\09.1-CONTEXT.md
@C:\Projects\KoorFameus\.planning\phases\09.1-design-change\09.1-RESEARCH.md
@C:\Projects\KoorFameus\app\page.tsx
@C:\Projects\KoorFameus\app\contact\page.tsx
@C:\Projects\KoorFameus\app\koren\page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage (app/page.tsx)</name>
  <files>app/page.tsx</files>
  <action>
  The homepage has 25 coral references. Replace all systematically:

  **Hero Section:**
  1. Badge icon: `text-coral` -> `text-purple`
  2. Hero heading accent: `text-coral` -> `text-purple`
  3. CTA button: `bg-coral hover:bg-coral-dark` -> `bg-purple hover:bg-purple-dark`

  **About Section:**
  4. Section label: `text-coral` -> `text-purple`
  5. Feature icons (4x): `bg-coral/10` and `text-coral` -> `bg-purple/10` and `text-purple`
  6. Decorative element: `bg-coral/10` -> `bg-purple/10`

  **Koren Section:**
  7. Section label: `text-coral` -> `text-purple`
  8. Fallback gradient: `from-coral to-coral-dark` -> `from-purple to-purple-dark`
  9. Card hover: `group-hover:text-coral` -> `group-hover:text-purple`
  10. Arrow link: `text-coral` -> `text-purple`
  11. CTA button: `border-coral text-coral hover:bg-coral` -> `border-purple text-purple hover:bg-purple`

  **Events Section:**
  12. Section label: `text-coral` -> `text-purple`
  13. Date badge: `bg-coral` -> `bg-purple`
  14. Event card hover: `hover:border-coral/30` -> `hover:border-purple/30`
  15. Arrow link: `text-coral` -> `text-purple`

  **CTA Section (full-width purple):**
  16. Background: `bg-coral` -> `bg-purple`
  17. Button: `text-coral hover:text-coral` -> `text-purple hover:text-purple`

  After changes, verify by searching for any remaining "coral" in the file.
  </action>
  <verify>
  Run `grep -c "coral" app/page.tsx` - should return 0.
  Run `grep -c "purple" app/page.tsx` - should return 20+.
  </verify>
  <done>
  Homepage uses purple throughout: hero, about section, koren cards, events, and CTA section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update remaining page files</name>
  <files>app/contact/page.tsx, app/koren/page.tsx, app/koren/[slug]/page.tsx, app/wie-zijn-wij/page.tsx, app/evenementen/page.tsx, app/galerij/page.tsx, app/galerij/[slug]/page.tsx, app/privacy/page.tsx, app/not-found.tsx</files>
  <action>
  For each page, read the file and replace all coral references with purple:

  **app/contact/page.tsx** (10 coral references):
  - Page header: `text-coral` -> `text-purple`
  - Icon backgrounds: `bg-coral/10` -> `bg-purple/10`
  - Icon colors: `text-coral` -> `text-purple`
  - Link colors: `text-coral hover:text-coral-dark` -> `text-purple hover:text-purple-dark`
  - FAQ card background: `bg-coral` -> `bg-purple`

  **app/koren/page.tsx** (8 coral references):
  - Section labels and accents
  - Card hovers and CTAs
  - Icon colors

  **app/koren/[slug]/page.tsx** (10 coral references):
  - Page header accent
  - Schedule section styling
  - CTA buttons
  - Icon colors

  **app/wie-zijn-wij/page.tsx** (8 coral references):
  - Section headers
  - Team card accents
  - Icon colors

  **app/evenementen/page.tsx** (10 coral references):
  - Date badges: `bg-coral` -> `bg-purple`
  - Section labels
  - Card hovers
  - Icon colors

  **app/galerij/page.tsx** (6 coral references):
  - Page header
  - Album card accents
  - Navigation

  **app/galerij/[slug]/page.tsx** (3 coral references):
  - Back link
  - Page title accent
  - Any card styling

  **app/privacy/page.tsx** (1 coral reference):
  - Page header accent

  **app/not-found.tsx** (3 coral references):
  - Heading accent
  - CTA button
  - Link colors
  </action>
  <verify>
  Run `grep -r "coral" app/ --include="*.tsx"` - should return 0 results.
  Run `npm run build` - should complete without errors.
  </verify>
  <done>
  All page files use purple. Zero coral references in app/ directory.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete visual redesign from coral to purple color palette</what-built>
  <how-to-verify>
  1. Run `npm run dev` and open http://localhost:3000

  2. **Homepage verification:**
     - Hero section: Purple "Fameus" text, purple CTA button
     - About section: Purple icons, purple section labels
     - Koren section: Purple hovers, purple CTA
     - Events section: Purple date badges
     - Bottom CTA: Full purple background

  3. **Header verification:**
     - "Koor Fameus" logo text is purple
     - Active nav item has purple background/text
     - Hover states show purple

  4. **Typography verification:**
     - h1, h2, h3 use Cormorant Garamond (elegant serif)
     - Body text uses Geist Sans

  5. **Other pages (spot check):**
     - /koren - purple accents
     - /contact - purple icons and form styling
     - /evenementen - purple date badges

  6. **Visual quality check:**
     - Colors feel cohesive (purple + orange accent)
     - Shadows feel soft and elegant
     - Background has warm cream tone
     - Overall impression: elegant, musical, child-friendly
  </how-to-verify>
  <resume-signal>Type "approved" if design looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "coral" app/ components/ --include="*.tsx" --include="*.css"` returns 0 results
2. `npm run build` passes without errors
3. Visual verification confirms purple theming throughout
4. Typography shows Cormorant Garamond on headings
</verification>

<success_criteria>
- All pages use purple color palette
- Zero coral references in entire codebase (app/, components/)
- Headings display in Cormorant Garamond
- Visual verification approved by user
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-design-change/09.1-03-SUMMARY.md`
</output>
