---
phase: 09.1-design-change
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/ui/button.tsx
autonomous: true

must_haves:
  truths:
    - "Purple (violet-500) is the new primary color throughout the site"
    - "Orange-500 is available as an accent color"
    - "Headings use elegant Cormorant Garamond font"
    - "Body text remains Geist Sans"
    - "Soft layered shadows are available for cards"
    - "Warm cream background is used for section backgrounds"
  artifacts:
    - path: "app/globals.css"
      provides: "Purple color palette, soft shadows, font variables"
      contains: "--color-purple"
    - path: "app/layout.tsx"
      provides: "Cormorant Garamond font import and CSS variable"
      contains: "Cormorant_Garamond"
    - path: "components/ui/button.tsx"
      provides: "Purple button variants replacing coral"
      contains: "bg-purple"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "--font-cormorant CSS variable"
      pattern: "font-cormorant"
    - from: "components/ui/button.tsx"
      to: "app/globals.css"
      via: "purple color classes"
      pattern: "bg-purple|text-purple"
---

<objective>
Establish the new design foundation by updating the core theme tokens (colors, fonts, shadows) and primary interactive component (button).

Purpose: This creates the design system that all other components and pages will consume. Must be completed first so subsequent plans can use the new classes.

Output: Updated globals.css with purple palette, Cormorant Garamond font in layout.tsx, button.tsx with purple variants.
</objective>

<execution_context>
@C:\Users\lieve\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\lieve\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Projects\KoorFameus\.planning\PROJECT.md
@C:\Projects\KoorFameus\.planning\phases\09.1-design-change\09.1-CONTEXT.md
@C:\Projects\KoorFameus\.planning\phases\09.1-design-change\09.1-RESEARCH.md
@C:\Projects\KoorFameus\app\globals.css
@C:\Projects\KoorFameus\app\layout.tsx
@C:\Projects\KoorFameus\components\ui\button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update globals.css with purple palette and soft shadows</name>
  <files>app/globals.css</files>
  <action>
  Replace the coral color palette with purple (violet) as primary:

  1. In @theme block, replace coral colors:
     - `--color-coral: #F3504C` -> `--color-purple: oklch(0.606 0.25 292.717)` (violet-500)
     - `--color-coral-dark: #D34153` -> `--color-purple-dark: oklch(0.541 0.281 293.009)` (violet-600)
     - `--color-coral-light: #F8807D` -> `--color-purple-light: oklch(0.702 0.183 293.541)` (violet-400)
     - Add `--color-purple-50: oklch(0.969 0.016 293.756)` for subtle backgrounds

  2. Update orange to use OKLCH:
     - `--color-orange: oklch(0.705 0.213 47.604)` (orange-500)
     - Add `--color-orange-dark: oklch(0.646 0.222 41.116)` (orange-600)

  3. Update background colors for warm cream:
     - `--color-bg-section: oklch(0.985 0.012 85)` (warm cream)
     - `--color-bg-accent: oklch(0.975 0.018 80)` (slightly warmer)

  4. Add soft shadow tokens in @theme:
     ```css
     --shadow-soft-sm: 0 1px 1px oklch(0.3 0.02 290 / 0.04), 0 2px 2px oklch(0.3 0.02 290 / 0.04);
     --shadow-soft-md: 0 1px 1px oklch(0.3 0.02 290 / 0.03), 0 2px 2px oklch(0.3 0.02 290 / 0.03), 0 4px 4px oklch(0.3 0.02 290 / 0.03), 0 8px 8px oklch(0.3 0.02 290 / 0.03);
     --shadow-soft-lg: 0 1px 1px oklch(0.3 0.02 290 / 0.02), 0 2px 2px oklch(0.3 0.02 290 / 0.02), 0 4px 4px oklch(0.3 0.02 290 / 0.02), 0 8px 8px oklch(0.3 0.02 290 / 0.02), 0 16px 16px oklch(0.3 0.02 290 / 0.02);
     ```

  5. Add @theme inline for font-heading:
     ```css
     --font-heading: var(--font-cormorant), Georgia, serif;
     ```

  6. Update :root shadcn variables:
     - `--primary: oklch(0.606 0.25 292.717)` (purple)
     - `--ring: oklch(0.606 0.25 292.717)` (purple for focus)
     - `--sidebar-primary: oklch(0.606 0.25 292.717)`
     - `--sidebar-ring: oklch(0.606 0.25 292.717)`
     - `--background: oklch(0.985 0.012 85)` (warm cream)

  7. Update focus styles:
     - Change `outline: 2px solid var(--color-coral)` to `outline: 2px solid var(--color-purple)`

  8. Update text gradients:
     - `.text-gradient-coral` -> `.text-gradient-purple` with purple gradient
     - Update `.text-gradient-rainbow` to use purple instead of coral

  9. Add base heading styles:
     ```css
     @layer base {
       h1, h2, h3 {
         font-family: var(--font-heading);
       }
     }
     ```

  10. Add utility classes for soft shadows:
      ```css
      @layer utilities {
        .shadow-soft-sm { box-shadow: var(--shadow-soft-sm); }
        .shadow-soft-md { box-shadow: var(--shadow-soft-md); }
        .shadow-soft-lg { box-shadow: var(--shadow-soft-lg); }
      }
      ```
  </action>
  <verify>
  Run `npm run build` - should complete without CSS errors.
  Grep for "coral" in globals.css - should find 0 results.
  Grep for "--color-purple" - should find the new color definitions.
  </verify>
  <done>
  globals.css contains purple color palette, soft shadow tokens, font-heading variable, and all coral references replaced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Cormorant Garamond font to layout.tsx</name>
  <files>app/layout.tsx</files>
  <action>
  1. Import Cormorant_Garamond from next/font/google:
     ```tsx
     import { Cormorant_Garamond } from "next/font/google";
     ```

  2. Configure the font with appropriate weights:
     ```tsx
     const cormorant = Cormorant_Garamond({
       subsets: ["latin"],
       weight: ["400", "500", "600", "700"],
       variable: "--font-cormorant",
       display: "swap",
     });
     ```

  3. Add the font variable to the html element:
     ```tsx
     <html lang="nl" className={`${GeistSans.className} ${cormorant.variable}`}>
     ```

  4. Update body className to use cream background:
     ```tsx
     <body className="antialiased bg-bg-cream text-text-primary min-h-screen flex flex-col">
     ```
     Note: Or use `bg-background` if globals.css --background is set to cream.
  </action>
  <verify>
  Run `npm run dev` and open browser DevTools.
  Inspect an h1/h2/h3 element - should show Cormorant Garamond in computed font-family.
  Check that --font-cormorant CSS variable exists in :root.
  </verify>
  <done>
  Cormorant Garamond is loaded via next/font, CSS variable is available, headings use the elegant serif font.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update button.tsx with purple variants</name>
  <files>components/ui/button.tsx</files>
  <action>
  Replace coral button variants with purple:

  1. Change `coral` variant to `purple`:
     ```tsx
     purple:
       "bg-purple text-white hover:bg-purple-dark shadow-sm transition-colors duration-200",
     ```

  2. Change `coral-outline` variant to `purple-outline`:
     ```tsx
     "purple-outline":
       "border-2 border-purple text-purple hover:bg-purple hover:text-white transition-colors duration-200",
     ```

  3. Add new `accent` variant for orange CTAs (used sparingly):
     ```tsx
     accent:
       "bg-orange text-white hover:bg-orange-dark shadow-sm transition-colors duration-200",
     ```

  4. Ensure the variant type includes these options (TypeScript will infer from cva).
  </action>
  <verify>
  Run `npm run build` - should complete without TypeScript errors.
  Grep for "coral" in button.tsx - should find 0 results.
  </verify>
  <done>
  Button component has purple, purple-outline, and accent variants. No coral references remain.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` passes without errors
2. `grep -r "coral" app/globals.css components/ui/button.tsx` returns 0 results
3. `grep -r "purple" app/globals.css` returns color definitions
4. Dev server shows Cormorant Garamond on headings
</verification>

<success_criteria>
- Purple color palette (violet-500/600/400) defined in globals.css
- Orange accent color defined with OKLCH values
- Soft shadow tokens (sm, md, lg) available
- Cormorant Garamond font loads and applies to h1-h3
- Button component has purple variants, no coral references
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-design-change/09.1-01-SUMMARY.md`
</output>
