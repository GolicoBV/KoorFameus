---
phase: 09.1-design-change
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/header.tsx
  - components/footer.tsx
  - components/contact-form.tsx
  - components/cookie-consent.tsx
  - components/google-map.tsx
  - components/ui/card.tsx
autonomous: true

must_haves:
  truths:
    - "Header uses purple for active nav states and logo text"
    - "Footer uses purple for social icons and hover states"
    - "Contact form uses purple for focus states and submit button"
    - "Cookie consent uses purple styling"
    - "Cards have soft shadow styling"
  artifacts:
    - path: "components/header.tsx"
      provides: "Purple nav active states, purple logo text"
      contains: "text-purple"
    - path: "components/footer.tsx"
      provides: "Purple social icons and link hovers"
      contains: "text-purple"
    - path: "components/contact-form.tsx"
      provides: "Purple submit button and focus states"
      contains: "purple"
    - path: "components/ui/card.tsx"
      provides: "Soft shadow option for cards"
      contains: "shadow-soft"
  key_links:
    - from: "components/header.tsx"
      to: "app/globals.css"
      via: "purple color classes"
      pattern: "text-purple|bg-purple"
    - from: "components/footer.tsx"
      to: "app/globals.css"
      via: "purple color classes"
      pattern: "text-purple|bg-purple"
---

<objective>
Update all shared components (header, footer, forms, cards) to use the new purple color palette.

Purpose: These components appear on every page. Updating them ensures consistent purple branding site-wide.

Output: All shared components using purple instead of coral, cards with soft shadow option.
</objective>

<execution_context>
@C:\Users\lieve\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\lieve\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Projects\KoorFameus\.planning\phases\09.1-design-change\09.1-CONTEXT.md
@C:\Projects\KoorFameus\.planning\phases\09.1-design-change\09.1-RESEARCH.md
@C:\Projects\KoorFameus\components\header.tsx
@C:\Projects\KoorFameus\components\footer.tsx
@C:\Projects\KoorFameus\components\contact-form.tsx
@C:\Projects\KoorFameus\components\cookie-consent.tsx
@C:\Projects\KoorFameus\components\google-map.tsx
@C:\Projects\KoorFameus\components\ui\card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update header.tsx with purple styling</name>
  <files>components/header.tsx</files>
  <action>
  Replace all coral references with purple:

  1. Logo text color:
     - `text-coral` -> `text-purple`

  2. Desktop nav active state:
     - `bg-coral/10 text-coral` -> `bg-purple/10 text-purple`

  3. Desktop nav hover state:
     - `hover:text-coral hover:bg-coral/5` -> `hover:text-purple hover:bg-purple/5`

  4. Mobile nav active state:
     - `bg-coral/10 text-coral` -> `bg-purple/10 text-purple`

  5. Mobile nav hover state:
     - `hover:text-coral hover:bg-coral/5` -> `hover:text-purple hover:bg-purple/5`

  The header has 5 coral references total that need updating.
  </action>
  <verify>
  Run `grep -c "coral" components/header.tsx` - should return 0.
  Run `grep -c "purple" components/header.tsx` - should return 5 or more.
  </verify>
  <done>
  Header uses purple for logo text, active nav states, and hover states. No coral references remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update footer.tsx and remaining components</name>
  <files>components/footer.tsx, components/contact-form.tsx, components/cookie-consent.tsx, components/google-map.tsx</files>
  <action>
  **footer.tsx** (5 coral references):
  1. Social icon buttons:
     - `bg-coral/10 text-coral hover:bg-coral` -> `bg-purple/10 text-purple hover:bg-purple`
  2. Quick links hover:
     - `hover:text-coral` -> `hover:text-purple`
  3. Contact email hover:
     - `hover:text-coral` -> `hover:text-purple`
  4. Privacy link hover:
     - `hover:text-coral` -> `hover:text-purple`

  **contact-form.tsx** (7 coral references):
  Read the file first, then replace all coral with purple:
  - Submit button styling
  - Focus ring colors
  - Error states (if any use coral)
  - Loading states

  **cookie-consent.tsx** (2 coral references):
  Read the file first, then replace:
  - Accept button styling
  - Any coral accent colors

  **google-map.tsx** (2 coral references):
  Read the file first, then replace:
  - Any coral-colored elements (likely loading states or placeholders)
  </action>
  <verify>
  Run for each file:
  - `grep -c "coral" components/footer.tsx` - should return 0
  - `grep -c "coral" components/contact-form.tsx` - should return 0
  - `grep -c "coral" components/cookie-consent.tsx` - should return 0
  - `grep -c "coral" components/google-map.tsx` - should return 0
  </verify>
  <done>
  All shared components (footer, contact-form, cookie-consent, google-map) use purple. No coral references in any component.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add soft shadow variant to card.tsx</name>
  <files>components/ui/card.tsx</files>
  <action>
  Update the Card component to support soft shadows:

  1. Read the current card.tsx implementation.

  2. If using cva (class-variance-authority), add a shadow variant:
     ```tsx
     const cardVariants = cva(
       "rounded-xl border bg-card text-card-foreground",
       {
         variants: {
           shadow: {
             none: "",
             default: "shadow-md",
             soft: "shadow-soft-md border-0",
             "soft-lg": "shadow-soft-lg border-0",
           },
         },
         defaultVariants: {
           shadow: "default",
         },
       }
     );
     ```

  3. If not using cva, simply ensure the default Card uses `shadow-md` and document that `shadow-soft-md` class can be added manually.

  4. The key is ensuring cards throughout the site can use the soft shadow system.

  Note: If the card.tsx is a simple component without variants, just add a comment noting that `shadow-soft-md` and `shadow-soft-lg` classes are available from globals.css for elevated cards.
  </action>
  <verify>
  Run `npm run build` - should complete without TypeScript errors.
  Check that card.tsx compiles correctly.
  </verify>
  <done>
  Card component is compatible with soft shadow classes. Can be applied via className prop.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` passes without errors
2. `grep -r "coral" components/` returns 0 results (excluding node_modules)
3. All shared components use purple color classes
</verification>

<success_criteria>
- Header: purple logo text, purple active/hover nav states
- Footer: purple social icons, purple link hovers
- Contact form: purple submit button, purple focus states
- Cookie consent: purple button styling
- Google map: no coral references
- Card: soft shadow classes available
- Zero coral references in components/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-design-change/09.1-02-SUMMARY.md`
</output>
